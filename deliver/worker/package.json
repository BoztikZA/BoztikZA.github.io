{
  "name": "boztik-deliver-v2-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply boztik_deliver_v2 --local",
    "db:migrate:remote": "wrangler d1 migrations apply boztik_deliver_v2 --remote"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250909.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
